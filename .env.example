# === QuickBooks API Credentials ===
QBO_CLIENT_ID=your-client-id-here
QBO_CLIENT_SECRET=your-client-secret-here
QBO_REDIRECT_URI=http://localhost:8000/callback

# This is only used if you're testing with a single company at a time.
QBO_REALM_ID=123456789012345

# === PostgreSQL Database Configuration ===
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pulsedb
DB_USER=pulseuser
DB_PASS=changeme

# === App Config ===
APP_SECRET_KEY=change-this-key
ENV=development

# === OAuth2 Token Storage ===
# (Used if you store tokens in a file — not recommended for production)
TOKEN_STORAGE_PATH=./tokens/

# === Risk Detection Thresholds (Optional Defaults) ===
RISK_LATE_INVOICE_DAYS=30
RISK_VENDOR_ALERT_AMOUNT=5000

# === Scheduler Settings ===
SYNC_INTERVAL_CRON=0 3 * * 1  # Every Monday at 3:00 AM

# === Logging ===
LOG_LEVEL=info
